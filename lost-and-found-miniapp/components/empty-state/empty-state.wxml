<view class="empty-state flex-col flex-center">
  <text class="empty-text text-secondary">{{text}}</text>
  <button
    wx:if="{{actionText}}"
    class="empty-action btn btn-outline btn-sm"
    bindtap="onAction"
  >
    {{actionText}}
  </button>
</view>
